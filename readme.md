# Simple Login Node JS app

### Using prisma orm | mysql | express

To initialize this repository input the next commands in terminal

`git clone "the url of this repository"`

`npm install`

`npm start`
